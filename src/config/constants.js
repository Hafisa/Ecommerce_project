'use strict';

const baseURL = '';

const scene = {
 login:"login",
  home: 'home',
  signup:'signup',
  UpdateScreen:'UpdateScreen'

};

const constants = {
  baseURL,
  scene,
};

export default constants;
