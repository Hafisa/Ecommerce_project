import React from 'react'
import { View, Text } from 'react-native'

const ButtonBox = (props) => {
    return (
        <View>
            <Text>{props.title} (Button Box type)</Text>
        </View>
    )
}

export default ButtonBox
